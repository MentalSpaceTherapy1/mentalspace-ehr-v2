# 🏗️ Module 7 Architecture Overview for Cursor

## 📐 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                        │
│                    http://localhost:5175                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                     App.tsx (Main Router)                │  │
│  │  - Route definitions (lines 850-944)                     │  │
│  │  - Authentication guards (PrivateRoute, PortalRoute)     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  Layout.tsx (Navigation)                 │  │
│  │  - Sidebar menu (lines 112-159)                          │  │
│  │  - 4 new submenus for Module 7                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              MODULE 7 COMPONENTS (12 Total)              │  │
│  │                                                           │  │
│  │  Progress Tracking:                                      │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ SymptomDiary.tsx    (1,249 lines)                  │  │  │
│  │  │ SleepDiary.tsx      (1,185 lines)                  │  │  │
│  │  │ ExerciseLog.tsx     (1,124 lines)                  │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  Self-Scheduling:                                        │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ PortalSelfScheduling.tsx (1,242 lines)             │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  Guardian Portal:                                        │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ GuardianPortal.tsx       (1,098 lines)             │  │  │
│  │  │ RequestAccess.tsx        (  892 lines)             │  │  │
│  │  │ GuardianConsent.tsx      (  987 lines)             │  │  │
│  │  │ GuardianVerification.tsx (1,156 lines) [Admin]     │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  Admin Tools:                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ SessionRatings.tsx       (  N/A lines)             │  │  │
│  │  │ CrisisDetections.tsx     (  N/A lines)             │  │  │
│  │  │ SchedulingRules.tsx      (1,189 lines)             │  │  │
│  │  │ WaitlistManagement.tsx   (1,124 lines)             │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  Clinician Tools:                                        │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ ClientProgress.tsx  (1,042 lines)                  │  │  │
│  │  │ MyWaitlist.tsx      (  953 lines)                  │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              │ HTTP/REST API                    │
│                              ▼                                  │
└─────────────────────────────────────────────────────────────────┘
                               │
                               │
┌─────────────────────────────────────────────────────────────────┐
│                        BACKEND (Express)                        │
│                     http://localhost:3001                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              routes/index.ts (Main Router)               │  │
│  │  - Crisis Detection: /api/crisis                         │  │
│  │  - Self-Scheduling: /api/self-schedule                   │  │
│  │  - Scheduling Rules: /api/scheduling-rules               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                      CONTROLLERS                         │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ crisis-detection.controller.ts                     │  │  │
│  │  │ self-scheduling.controller.ts                      │  │  │
│  │  │ scheduling-rules.controller.ts                     │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                       SERVICES                           │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ crisis-detection.service.ts                        │  │  │
│  │  │ symptom-tracking.service.ts                        │  │  │
│  │  │ sleep-tracking.service.ts                          │  │  │
│  │  │ exercise-tracking.service.ts                       │  │  │
│  │  │ available-slots.service.ts                         │  │  │
│  │  │ scheduling-rules.service.ts                        │  │  │
│  │  │ guardian-relationship.service.ts                   │  │  │
│  │  │ progress-analytics.service.ts                      │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              │ Prisma ORM                       │
│                              ▼                                  │
└─────────────────────────────────────────────────────────────────┘
                               │
                               │
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE (PostgreSQL)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    CORE MODELS                           │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ Client (lines 423-562)                             │  │  │
│  │  │  - id, medicalRecordNumber, demographics           │  │  │
│  │  │  - primaryTherapistId → User                       │  │  │
│  │  │  - 1:1 PortalAccount                               │  │  │
│  │  │  - 1:N SymptomLog, SleepLog, ExerciseLog          │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ PortalAccount (lines 1843-1882)                    │  │  │
│  │  │  - id, clientId, email, password (hashed)          │  │  │
│  │  │  - accountStatus, emailVerified                    │  │  │
│  │  │  - portalAccessGranted, mfaEnabled                 │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                MODULE 7 TRACKING MODELS                  │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ SymptomLog                                         │  │  │
│  │  │  - clientId, date, symptom, severity, notes        │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ SleepLog                                           │  │  │
│  │  │  - clientId, date, bedtime, waketime, quality      │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ ExerciseLog                                        │  │  │
│  │  │  - clientId, date, type, duration, intensity       │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              MODULE 7 SCHEDULING MODELS                  │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ SchedulingRule                                     │  │  │
│  │  │  - therapistId, appointmentType, duration          │  │  │
│  │  │  - bufferTime, maxPerDay                           │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ ClientWaitlist                                     │  │  │
│  │  │  - clientId, therapistId, priority, status         │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │               MODULE 7 GUARDIAN MODELS                   │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ GuardianRelationship                               │  │  │
│  │  │  - guardianId, dependentId, relationship           │  │  │
│  │  │  - status, accessLevel                             │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ GuardianAccessRequest                              │  │  │
│  │  │  - requesterId, dependentId, status                │  │  │
│  │  │  - verificationStatus, documents                   │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### Progress Tracking Flow

```
┌────────────┐                    ┌──────────────┐
│   Client   │                    │   Clinician  │
│  (Portal)  │                    │  (Dashboard) │
└─────┬──────┘                    └──────┬───────┘
      │                                  │
      │ 1. Enter symptom data            │
      │    (SymptomDiary.tsx)            │
      ▼                                  │
┌────────────────────┐                   │
│ POST /api/symptoms │                   │
└─────┬──────────────┘                   │
      │                                  │
      │ 2. Save to database              │
      ▼                                  │
┌──────────────┐                         │
│  SymptomLog  │                         │
│   (Table)    │                         │
└─────┬────────┘                         │
      │                                  │
      │ 3. Clinician views progress      │
      │                                  │
      └──────────────────────────────────▼
                           ┌────────────────────────┐
                           │ GET /api/client-progress│
                           └────────────────────────┘
                                        │
                                        ▼
                           ┌────────────────────────┐
                           │  ClientProgress.tsx    │
                           │  - Charts & graphs     │
                           │  - Trend analysis      │
                           └────────────────────────┘
```

### Self-Scheduling Flow

```
┌────────────┐
│   Client   │
│  (Portal)  │
└─────┬──────┘
      │
      │ 1. View available slots
      ▼
┌─────────────────────────────┐
│ PortalSelfScheduling.tsx    │
│  - Calendar view             │
│  - Available times           │
└─────┬───────────────────────┘
      │
      │ 2. Request available slots
      ▼
┌──────────────────────────────┐
│ GET /api/self-schedule/slots │
└─────┬────────────────────────┘
      │
      │ 3. Apply scheduling rules
      ▼
┌──────────────────────┐        ┌───────────────┐
│AvailableSlotsService │───────▶│SchedulingRule│
└─────┬────────────────┘        └───────────────┘
      │
      │ 4. Return available slots
      ▼
┌────────────┐
│   Client   │
│  Selects   │
│    Slot    │
└─────┬──────┘
      │
      │ 5. Book appointment
      ▼
┌────────────────────────────────┐
│ POST /api/self-schedule/book   │
└─────┬──────────────────────────┘
      │
      │ 6. Create appointment
      ▼
┌─────────────┐
│ Appointment │
│   (Table)   │
└─────────────┘
```

### Guardian Access Request Flow

```
┌─────────────┐     ┌───────────┐     ┌─────────────┐
│  Guardian   │     │   Admin   │     │   Client    │
│ (Requester) │     │(Verifier) │     │(Dependent)  │
└──────┬──────┘     └─────┬─────┘     └──────┬──────┘
       │                  │                   │
       │ 1. Request access│                   │
       ▼                  │                   │
┌────────────────┐        │                   │
│RequestAccess   │        │                   │
│    .tsx        │        │                   │
└──────┬─────────┘        │                   │
       │                  │                   │
       │ 2. Submit request│                   │
       ▼                  │                   │
┌──────────────────────┐  │                   │
│GuardianAccessRequest │  │                   │
│      (Table)         │  │                   │
└──────┬───────────────┘  │                   │
       │                  │                   │
       │                  │ 3. Admin reviews  │
       │                  ▼                   │
       │        ┌─────────────────────┐       │
       │        │GuardianVerification │       │
       │        │       .tsx          │       │
       │        └─────────┬───────────┘       │
       │                  │                   │
       │                  │ 4. Approve/Deny   │
       │                  ▼                   │
       │        ┌─────────────────────┐       │
       │        │Update request status│       │
       │        └─────────┬───────────┘       │
       │                  │                   │
       │                  │ 5. If approved    │
       │                  │                   │
       │                  └───────────────────▶│
       │                        6. Client     │
       │                           consents   │
       │                                      ▼
       │                          ┌──────────────────┐
       │                          │GuardianConsent   │
       │                          │     .tsx         │
       │                          └────────┬─────────┘
       │                                   │
       │                                   │ 7. Grant access
       │                                   ▼
       │                          ┌───────────────────┐
       │                          │GuardianRelationship│
       │◀─────────────────────────│  status: ACTIVE   │
       │  8. Access granted       └───────────────────┘
       │
       ▼
┌──────────────┐
│GuardianPortal│
│    .tsx      │
│ Can now view │
│ dependent    │
│    data      │
└──────────────┘
```

---

## 🗂️ File Organization

```
mentalspace-ehr-v2/
│
├── packages/
│   │
│   ├── frontend/
│   │   └── src/
│   │       ├── App.tsx ⭐ (Routes: 850-944)
│   │       ├── components/
│   │       │   └── Layout.tsx ⭐ (Nav: 112-159)
│   │       │
│   │       └── pages/
│   │           ├── Client/ ⭐
│   │           │   ├── SymptomDiary.tsx
│   │           │   ├── SleepDiary.tsx
│   │           │   └── ExerciseLog.tsx
│   │           │
│   │           ├── Portal/ ⭐
│   │           │   └── PortalSelfScheduling.tsx
│   │           │
│   │           ├── Guardian/ ⭐
│   │           │   ├── GuardianPortal.tsx
│   │           │   ├── RequestAccess.tsx
│   │           │   └── GuardianConsent.tsx
│   │           │
│   │           ├── Admin/ ⭐
│   │           │   ├── SessionRatings.tsx
│   │           │   ├── CrisisDetections.tsx
│   │           │   ├── GuardianVerification.tsx
│   │           │   ├── SchedulingRules.tsx
│   │           │   └── WaitlistManagement.tsx
│   │           │
│   │           └── Clinician/ ⭐
│   │               ├── ClientProgress.tsx
│   │               └── MyWaitlist.tsx
│   │
│   ├── backend/
│   │   └── src/
│   │       ├── routes/
│   │       │   ├── index.ts ⭐ (191-198)
│   │       │   ├── crisis-detection.routes.ts ⭐
│   │       │   ├── self-scheduling.routes.ts ⭐
│   │       │   └── scheduling-rules.routes.ts ⭐
│   │       │
│   │       ├── controllers/ ⭐
│   │       │   ├── crisis-detection.controller.ts
│   │       │   ├── self-scheduling.controller.ts
│   │       │   └── scheduling-rules.controller.ts
│   │       │
│   │       ├── services/ ⭐
│   │       │   ├── crisis-detection.service.ts
│   │       │   ├── symptom-tracking.service.ts
│   │       │   ├── sleep-tracking.service.ts
│   │       │   ├── exercise-tracking.service.ts
│   │       │   ├── available-slots.service.ts
│   │       │   ├── scheduling-rules.service.ts
│   │       │   ├── guardian-relationship.service.ts
│   │       │   └── progress-analytics.service.ts
│   │       │
│   │       └── middleware/
│   │           ├── guardian-access.middleware.ts ⭐
│   │           └── roleCheck.ts
│   │
│   └── database/
│       └── prisma/
│           └── schema.prisma ⭐
│               ├── Client (423-562)
│               ├── PortalAccount (1843-1882)
│               └── Module 7 Models (2800+)
│
├── Root Utilities: ⭐
│   ├── create-test-client.js
│   ├── verify-test-client.js
│   └── check-users.js
│
└── Documentation: ⭐
    ├── CURSOR_COMPREHENSIVE_PROJECT_STATUS.md
    ├── CURSOR_QUICK_REFERENCE.md
    ├── CURSOR_TESTING_CHECKLIST.md
    └── CURSOR_ARCHITECTURE_OVERVIEW.md (this file)
```

**Legend:**
⭐ = Module 7 files or important reference

---

## 🔐 Authentication & Authorization Flow

```
┌─────────────────────────────────────────────────────────────┐
│                      LOGIN REQUEST                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
              ┌────────────────────┐
              │ POST /api/auth/    │
              │ portal/login       │
              └─────────┬──────────┘
                        │
                        ▼
          ┌──────────────────────────┐
          │ Find PortalAccount       │
          │ by email                 │
          └────────┬─────────────────┘
                   │
                   ▼
          ┌──────────────────────────┐
          │ Verify password          │
          │ (bcrypt.compare)         │
          └────────┬─────────────────┘
                   │
                   ▼
          ┌──────────────────────────┐
          │ Check accountStatus      │
          │ Check emailVerified      │
          │ Check portalAccessGranted│
          └────────┬─────────────────┘
                   │
                   ▼
          ┌──────────────────────────┐
          │ Generate JWT tokens      │
          │ - Access token           │
          │ - Refresh token          │
          └────────┬─────────────────┘
                   │
                   ▼
          ┌──────────────────────────┐
          │ Return to client         │
          │ - tokens                 │
          │ - user data              │
          └──────────────────────────┘
```

### Protected Route Flow

```
┌─────────────────────────────────────────────────────────────┐
│              API REQUEST (with token)                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
              ┌────────────────────┐
              │ authMiddleware     │
              │ verifies JWT       │
              └─────────┬──────────┘
                        │
                        ├─ Invalid ──▶ 401 Unauthorized
                        │
                        ▼ Valid
              ┌────────────────────┐
              │ roleCheck          │
              │ middleware         │
              │ (if needed)        │
              └─────────┬──────────┘
                        │
                        ├─ No access ──▶ 403 Forbidden
                        │
                        ▼ Authorized
              ┌────────────────────┐
              │ Controller         │
              │ processes request  │
              └─────────┬──────────┘
                        │
                        ▼
              ┌────────────────────┐
              │ Return response    │
              └────────────────────┘
```

---

## 📊 Component State Management

### Example: SymptomDiary.tsx

```typescript
┌─────────────────────────────────────────────────────────┐
│                    SymptomDiary.tsx                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  State Variables:                                       │
│  ┌────────────────────────────────────────────────────┐ │
│  │ const [symptoms, setSymptoms] = useState([])       │ │
│  │ const [selectedDate, setSelectedDate] = useState() │ │
│  │ const [severity, setSeverity] = useState(5)        │ │
│  │ const [notes, setNotes] = useState('')             │ │
│  │ const [loading, setLoading] = useState(false)      │ │
│  │ const [error, setError] = useState(null)           │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  useEffect Hooks:                                       │
│  ┌────────────────────────────────────────────────────┐ │
│  │ useEffect(() => {                                  │ │
│  │   fetchSymptoms() // On mount & date change        │ │
│  │ }, [selectedDate])                                 │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  API Functions:                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │ fetchSymptoms() → GET /api/symptoms                │ │
│  │ createSymptom() → POST /api/symptoms               │ │
│  │ updateSymptom() → PUT /api/symptoms/:id            │ │
│  │ deleteSymptom() → DELETE /api/symptoms/:id         │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  Render:                                                │
│  ┌────────────────────────────────────────────────────┐ │
│  │ - Date picker                                      │ │
│  │ - Symptom input form                               │ │
│  │ - Severity slider                                  │ │
│  │ - Notes textarea                                   │ │
│  │ - Submit button                                    │ │
│  │ - Symptoms list                                    │ │
│  │ - Chart/graph visualization                        │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🌐 API Endpoint Summary

### Progress Tracking Endpoints

```
POST   /api/symptoms              - Create symptom entry
GET    /api/symptoms              - Get symptom entries
GET    /api/symptoms/:id          - Get specific entry
PUT    /api/symptoms/:id          - Update entry
DELETE /api/symptoms/:id          - Delete entry

POST   /api/sleep                 - Create sleep entry
GET    /api/sleep                 - Get sleep entries
GET    /api/sleep/:id             - Get specific entry
PUT    /api/sleep/:id             - Update entry
DELETE /api/sleep/:id             - Delete entry

POST   /api/exercise              - Create exercise entry
GET    /api/exercise              - Get exercise entries
GET    /api/exercise/:id          - Get specific entry
PUT    /api/exercise/:id          - Update entry
DELETE /api/exercise/:id          - Delete entry

GET    /api/client-progress/:id   - Get all tracking data for client
```

### Self-Scheduling Endpoints

```
GET    /api/self-schedule/slots   - Get available appointment slots
POST   /api/self-schedule/book    - Book an appointment
PUT    /api/self-schedule/:id     - Reschedule appointment
DELETE /api/self-schedule/:id     - Cancel appointment
GET    /api/self-schedule/my      - Get client's appointments
```

### Guardian Endpoints

```
POST   /api/guardian/request      - Submit access request
GET    /api/guardian/requests     - Get guardian's requests
GET    /api/guardian/dependents   - Get guardian's dependents
PUT    /api/guardian/relationship/:id - Update relationship

GET    /api/admin/guardian-requests   - Get all pending requests
PUT    /api/admin/guardian-verify/:id - Approve/deny request
```

### Admin Endpoints

```
GET    /api/scheduling-rules      - Get scheduling rules
POST   /api/scheduling-rules      - Create rule
PUT    /api/scheduling-rules/:id  - Update rule
DELETE /api/scheduling-rules/:id  - Delete rule

GET    /api/waitlist              - Get all waitlists
POST   /api/waitlist              - Add to waitlist
PUT    /api/waitlist/:id          - Update waitlist entry
DELETE /api/waitlist/:id          - Remove from waitlist

GET    /api/crisis                - Get crisis detections
PUT    /api/crisis/:id/reviewed   - Mark as reviewed
```

---

## 🎨 UI Component Patterns

### Common Layout Pattern

```tsx
const ComponentName = () => {
  // 1. State declarations
  const [data, setData] = useState([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  // 2. useEffect for data fetching
  useEffect(() => {
    fetchData();
  }, [dependencies]);

  // 3. API functions
  const fetchData = async () => {
    try {
      setLoading(true);
      const response = await axios.get('/api/endpoint');
      setData(response.data);
    } catch (err) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  };

  // 4. Event handlers
  const handleSubmit = async (e) => {
    e.preventDefault();
    // Handle form submission
  };

  // 5. Loading state
  if (loading) return <LoadingSpinner />;

  // 6. Error state
  if (error) return <ErrorMessage error={error} />;

  // 7. Main render
  return (
    <div className="container mx-auto p-6">
      <h1 className="text-2xl font-bold mb-4">Title</h1>
      {/* Content */}
    </div>
  );
};
```

---

## 🔍 Testing User Journeys

### Journey 1: Client Tracks Symptoms

```
1. Client logs in
   └─▶ PortalLogin.tsx
       └─▶ POST /api/auth/portal/login
           └─▶ Redirect to /portal/dashboard

2. Navigate to Symptom Diary
   └─▶ Click "Progress Tracking" → "Symptom Diary"
       └─▶ Route: /client/symptoms
           └─▶ SymptomDiary.tsx renders

3. View existing symptoms
   └─▶ useEffect fires on mount
       └─▶ GET /api/symptoms?clientId=xxx&date=...
           └─▶ Symptoms displayed in list/chart

4. Add new symptom
   └─▶ Fill form (symptom, severity, notes)
       └─▶ Click "Save"
           └─▶ POST /api/symptoms
               └─▶ Success message
                   └─▶ List refreshes with new entry
```

### Journey 2: Clinician Views Progress

```
1. Clinician logs in
   └─▶ Route: /login
       └─▶ POST /api/auth/login
           └─▶ Redirect to /dashboard

2. Navigate to Client Progress
   └─▶ Click "Clinician Tools" → "Client Progress"
       └─▶ Route: /clinician/client-progress
           └─▶ ClientProgress.tsx renders

3. Select client
   └─▶ Choose "John Doe" from dropdown
       └─▶ GET /api/client-progress/xxx
           └─▶ Returns all tracking data
               └─▶ Charts render with data

4. Analyze trends
   └─▶ View symptom chart
       └─▶ Adjust date range
           └─▶ Chart updates
               └─▶ Export option available
```

### Journey 3: Guardian Requests Access

```
1. Guardian (not yet registered) visits site
   └─▶ Registers as guardian
       └─▶ Route: /register
           └─▶ POST /api/auth/register
               └─▶ Account created

2. Request access to dependent
   └─▶ Route: /guardian/request-access
       └─▶ RequestAccess.tsx
           └─▶ Fill form (dependent info, relationship)
               └─▶ POST /api/guardian/request
                   └─▶ Request submitted

3. Admin reviews request
   └─▶ Admin logs in
       └─▶ Route: /admin/guardian-verification
           └─▶ GuardianVerification.tsx
               └─▶ Reviews documents
                   └─▶ PUT /api/admin/guardian-verify/xxx
                       └─▶ Request approved

4. Client (dependent) consents
   └─▶ Client logs in
       └─▶ Notification of pending request
           └─▶ Route: /client/guardian-consent
               └─▶ GuardianConsent.tsx
                   └─▶ Reviews request
                       └─▶ Click "Approve"
                           └─▶ POST /api/guardian/consent
                               └─▶ Relationship established

5. Guardian gains access
   └─▶ Guardian logs in
       └─▶ Route: /guardian/portal
           └─▶ GuardianPortal.tsx
               └─▶ Can now view dependent's data
```

---

## 📝 Quick Reference Summary

**Test Credentials:**
- Client: john.doe@example.com / TestClient123!
- Portal URL: http://localhost:5175/portal/login

**Module 7 Routes (12 total):**
- 3 Progress Tracking
- 1 Self-Scheduling
- 4 Guardian Portal
- 4 Admin Tools

**Key Technologies:**
- React + TypeScript
- Tailwind CSS
- Axios for API
- Prisma ORM
- PostgreSQL

**Documentation:**
- CURSOR_COMPREHENSIVE_PROJECT_STATUS.md (full details)
- CURSOR_QUICK_REFERENCE.md (quick facts)
- CURSOR_TESTING_CHECKLIST.md (test cases)
- CURSOR_ARCHITECTURE_OVERVIEW.md (this file)

---

**Last Updated:** 2025-11-09
