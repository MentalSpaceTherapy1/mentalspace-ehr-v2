# Custom Report Builder - System Architecture

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                                                                 │
│  ┌──────────────────┐                ┌──────────────────┐     │
│  │  Report Builder  │                │  Reports List    │     │
│  │     (Wizard)     │                │   (Dashboard)    │     │
│  │                  │                │                  │     │
│  │  • Data Sources  │                │  • My Reports    │     │
│  │  • Fields        │                │  • Templates     │     │
│  │  • Filters       │                │  • Shared        │     │
│  │  • Aggregations  │                │  • Search        │     │
│  │  • Preview       │                │                  │     │
│  │  • Save          │                │                  │     │
│  └──────────────────┘                └──────────────────┘     │
│                                                                 │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │ HTTP/REST API
                        │
┌───────────────────────▼─────────────────────────────────────────┐
│                      API LAYER (Express)                        │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │         Custom Reports Controller                       │   │
│  │  /api/v1/custom-reports/*                              │   │
│  │                                                         │   │
│  │  • create()      • execute()      • share()           │   │
│  │  • getAll()      • clone()        • getVersions()     │   │
│  │  • getById()     • validate()     • rollback()        │   │
│  │  • update()      • preview()                          │   │
│  │  • delete()      • templates()                        │   │
│  └────────────────────────────────────────────────────────┘   │
│                           │                                     │
└───────────────────────────┼─────────────────────────────────────┘
                            │
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                   BUSINESS LOGIC LAYER                          │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │         Query Builder Service                           │   │
│  │                                                         │   │
│  │  • getAvailableDataSources()                          │   │
│  │  • validateQueryConfig()                              │   │
│  │  • executeQuery()                                     │   │
│  │  • executeAggregationQuery()                          │   │
│  │  • executeGroupedAggregationQuery()                   │   │
│  │  • buildPrismaQuery()                                 │   │
│  │  • transformResults()                                 │   │
│  └────────────────────────────────────────────────────────┘   │
│                           │                                     │
│  ┌────────────────────────▼────────────────────────────────┐  │
│  │     Data Source Metadata                                │  │
│  │  • Client        • Charge         • Insurance          │  │
│  │  • Appointment   • ServiceCode    • Payer              │  │
│  │  • ClinicalNote  • User                                │  │
│  │                                                         │  │
│  │  For each: Fields, Types, Relations                    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ Prisma ORM
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                      DATA LAYER                                 │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  ReportDefinition Model                                 │   │
│  │  • id, userId, name, description                       │   │
│  │  • category, isPublic, isTemplate                      │   │
│  │  • queryConfig (JSON)                                  │   │
│  │  • timestamps                                          │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  ReportVersion Model                                    │   │
│  │  • id, reportId, versionNumber                         │   │
│  │  • queryConfig (JSON)                                  │   │
│  │  • changedBy, changeNote                               │   │
│  │  • timestamp                                           │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  Business Data Models (Queried)                        │   │
│  │  • Client        • Charge         • Insurance          │   │
│  │  • Appointment   • ServiceCode    • Payer              │   │
│  │  • ClinicalNote  • User                                │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Query Execution Flow

```
┌─────────────┐
│   User      │
│  Clicks     │
│  "Execute"  │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Frontend: POST /custom-reports/:id/execute  │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  Controller: executeReport()     │
│  • Fetch report definition       │
│  • Merge custom filters          │
│  • Determine query type          │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  Query Builder Service           │
│  • Validate config               │
│  • Build Prisma query            │
└──────┬───────────────────────────┘
       │
       ▼
   ┌───┴────┐
   │ Query  │
   │ Type?  │
   └───┬────┘
       │
   ┌───┼────────────────────────┐
   │   │                        │
   ▼   ▼                        ▼
┌──────────┐  ┌──────────────┐  ┌───────────────────┐
│ Standard │  │ Aggregation  │  │ Grouped           │
│ Query    │  │ Query        │  │ Aggregation       │
│          │  │              │  │ Query             │
└────┬─────┘  └─────┬────────┘  └────────┬──────────┘
     │              │                     │
     └──────────────┼─────────────────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │  Prisma Client       │
         │  • findMany()        │
         │  • aggregate()       │
         │  • groupBy()         │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │  Database (Postgres) │
         │  • Execute SQL       │
         │  • Return rows       │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │  Transform Results   │
         │  • Flatten relations │
         │  • Apply aliases     │
         │  • Format values     │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │  Return JSON         │
         │  {                   │
         │    report: {...},    │
         │    results: [...],   │
         │    executedAt: "..." │
         │  }                   │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │  Frontend Display    │
         │  • Table view        │
         │  • CSV export        │
         │  • Charts (future)   │
         └──────────────────────┘
```

---

## Query Config Transformation Pipeline

```
┌─────────────────────────────────────────────────────────┐
│                JSON Query Configuration                  │
│  {                                                       │
│    dataSources: ["Client", "Appointment"],              │
│    fields: [                                            │
│      { source: "Client", field: "firstName" },          │
│      { source: "Appointment", field: "status" }         │
│    ],                                                   │
│    filters: [                                           │
│      { field: "status", operator: "EQUALS",             │
│        values: ["COMPLETED"] }                          │
│    ],                                                   │
│    orderBy: [{ field: "firstName", direction: "ASC" }]  │
│  }                                                       │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│              Validation & Metadata Lookup               │
│  • Check data sources exist                             │
│  • Verify fields exist in sources                       │
│  • Validate operators                                   │
│  • Check relationships for joins                        │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│              Build Prisma Query Object                  │
│  {                                                       │
│    select: {                                            │
│      firstName: true,                                   │
│      appointments: {                                    │
│        select: { status: true }                         │
│      }                                                  │
│    },                                                   │
│    where: {                                             │
│      appointments: {                                    │
│        some: { status: 'COMPLETED' }                    │
│      }                                                  │
│    },                                                   │
│    orderBy: { firstName: 'asc' }                        │
│  }                                                       │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│              Execute via Prisma Client                  │
│  prisma.client.findMany({...})                          │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│              Database Query (SQL)                       │
│  SELECT                                                 │
│    c.first_name,                                        │
│    a.status                                             │
│  FROM clients c                                         │
│  JOIN appointments a ON a.client_id = c.id             │
│  WHERE a.status = 'COMPLETED'                           │
│  ORDER BY c.first_name ASC                              │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│              Transform & Flatten Results                │
│  Input:                                                 │
│  [                                                      │
│    {                                                    │
│      firstName: "John",                                 │
│      appointments: [{ status: "COMPLETED" }]            │
│    }                                                    │
│  ]                                                      │
│                                                         │
│  Output:                                                │
│  [                                                      │
│    {                                                    │
│      Client_firstName: "John",                          │
│      Appointment_status: "COMPLETED"                    │
│    }                                                    │
│  ]                                                      │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│              Return to Client                           │
└─────────────────────────────────────────────────────────┘
```

---

## Component Interaction Diagram

```
Frontend Components:
┌────────────────────────────────────────────────────────┐
│                                                        │
│  CustomReportBuilder (Main Wizard)                    │
│  ├─ Step 1: DataSourceSelector                        │
│  │           ↓ GET /data-sources                      │
│  │           ↓ Displays available tables              │
│  │                                                     │
│  ├─ Step 2: FieldSelector                             │
│  │           ↓ Shows fields from selected sources     │
│  │           ↓ User drags/clicks to select            │
│  │                                                     │
│  ├─ Step 3: FilterBuilder                             │
│  │           ↓ Dynamic filter rows                    │
│  │           ↓ Field + Operator + Value               │
│  │                                                     │
│  ├─ Step 4: AggregationBuilder                        │
│  │           ↓ GROUP BY field chips                   │
│  │           ↓ Aggregation function rows              │
│  │                                                     │
│  ├─ Step 5: SortBuilder (placeholder)                 │
│  │                                                     │
│  ├─ Step 6: ReportPreview                             │
│  │           ↓ POST /preview with queryConfig         │
│  │           ↓ Displays 10-row table                  │
│  │           ↓ CSV export button                      │
│  │                                                     │
│  └─ Step 7: Save                                       │
│             ↓ POST /custom-reports                     │
│             ↓ Creates ReportDefinition + Version       │
│                                                        │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                                                        │
│  CustomReportsList (Management Dashboard)             │
│  ├─ Tab: All Reports                                  │
│  │   ↓ GET /custom-reports?includePublic=true        │
│  │                                                     │
│  ├─ Tab: My Reports                                   │
│  │   ↓ Filters user's reports                         │
│  │                                                     │
│  ├─ Tab: Templates                                    │
│  │   ↓ GET /templates                                 │
│  │                                                     │
│  ├─ Tab: Shared                                       │
│  │   ↓ Filters isPublic=true                          │
│  │                                                     │
│  └─ Actions:                                          │
│      • Execute → POST /:id/execute                    │
│      • Edit → Navigate to builder                     │
│      • Clone → POST /:id/clone                        │
│      • Share → POST /:id/share                        │
│      • Delete → DELETE /:id                           │
│      • Versions → GET /:id/versions                   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## Data Model Relationships

```
┌──────────────────┐          ┌──────────────────┐
│      User        │◄─────────│ ReportDefinition │
│                  │  userId   │                  │
│  • id            │           │  • id            │
│  • email         │           │  • userId        │
│  • firstName     │           │  • name          │
│  • lastName      │           │  • description   │
│  • role          │           │  • category      │
└──────────────────┘           │  • isPublic      │
                               │  • isTemplate    │
                               │  • queryConfig   │
                               └────────┬─────────┘
                                        │
                                        │ reportId
                                        ▼
                               ┌──────────────────┐
                               │  ReportVersion   │
                               │                  │
                               │  • id            │
                               │  • reportId      │
                               │  • versionNumber │
                               │  • queryConfig   │
                               │  • changedBy     │
                               │  • changeNote    │
                               │  • createdAt     │
                               └──────────────────┘
```

---

## Query Configuration Schema

```json
{
  "dataSources": ["string"],        // Required: Table names
  "fields": [                        // Required: Selected fields
    {
      "source": "string",            // Table name
      "field": "string",             // Field name
      "alias": "string"              // Optional: Custom column name
    }
  ],
  "filters": [                       // Optional: WHERE conditions
    {
      "field": "string",             // Table.field or just field
      "operator": "string",          // EQUALS, IN, GT, BETWEEN, etc.
      "values": ["any"]              // Filter values (1 or 2 for BETWEEN)
    }
  ],
  "groupBy": ["string"],             // Optional: GROUP BY fields
  "aggregations": [                  // Optional: Aggregate functions
    {
      "field": "string",             // Field to aggregate
      "function": "string",          // COUNT, SUM, AVG, MIN, MAX
      "alias": "string"              // Required: Result column name
    }
  ],
  "orderBy": [                       // Optional: Sorting
    {
      "field": "string",             // Field or alias to sort by
      "direction": "ASC|DESC"        // Sort direction
    }
  ],
  "limit": "number",                 // Optional: Result limit
  "offset": "number"                 // Optional: Result offset
}
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   Security Layers                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Layer 1: Authentication                                │
│  ├─ JWT token required for all endpoints               │
│  ├─ Token contains userId and roles                    │
│  └─ Unauthenticated requests rejected                  │
│                                                         │
│  Layer 2: Authorization                                 │
│  ├─ Users can only see their own reports + public      │
│  ├─ Only report owners can edit/delete                 │
│  ├─ Sharing controlled by isPublic flag                │
│  └─ Templates visible to all authenticated users       │
│                                                         │
│  Layer 3: Query Validation                             │
│  ├─ Pre-execution validation of query config           │
│  ├─ Data source existence check                        │
│  ├─ Field existence verification                       │
│  ├─ Operator validation                                │
│  └─ Type checking on filter values                     │
│                                                         │
│  Layer 4: Query Execution                              │
│  ├─ Prisma ORM prevents SQL injection                  │
│  ├─ Parameterized queries                              │
│  ├─ Row-level security via Prisma filters              │
│  └─ Timeout protection                                 │
│                                                         │
│  Layer 5: Audit Trail                                  │
│  ├─ Version control tracks all changes                 │
│  ├─ ChangedBy field records user                       │
│  ├─ Timestamps on all operations                       │
│  └─ Rollback capability                                │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## Performance Optimization Strategies

```
┌─────────────────────────────────────────────────────────┐
│                Current Implementation                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ✅ Indexed Fields                                      │
│     • userId on ReportDefinition                        │
│     • category on ReportDefinition                      │
│     • reportId on ReportVersion                         │
│                                                         │
│  ✅ Query Validation                                    │
│     • Prevents execution of invalid queries             │
│     • Early error detection                             │
│                                                         │
│  ✅ Preview Limiting                                    │
│     • 10-row limit on previews                          │
│     • Fast feedback for users                           │
│                                                         │
│  ✅ Efficient Joins                                     │
│     • Prisma handles optimal JOIN generation            │
│     • Relation-based queries                            │
│                                                         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                  Future Optimizations                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  🔮 Result Caching                                      │
│     • Cache query results by config hash                │
│     • TTL-based invalidation                            │
│     • Reduce database load                              │
│                                                         │
│  🔮 Query Result Pagination                             │
│     • Server-side pagination for large results          │
│     • Streaming responses                               │
│                                                         │
│  🔮 Background Execution                                │
│     • Queue long-running reports                        │
│     • Email results when complete                       │
│                                                         │
│  🔮 Query Plan Analysis                                 │
│     • Analyze slow queries                              │
│     • Suggest indexes                                   │
│                                                         │
│  🔮 Materialized Views                                  │
│     • Pre-aggregate common queries                      │
│     • Refresh on schedule                               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
┌──────────────────┐
│  User Action     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Frontend        │
│  Validation      │ ──────┐
└────────┬─────────┘       │ Invalid input
         │                 ▼
         │            ┌────────────────┐
         │            │  Show Error    │
         │            │  Message       │
         │            └────────────────┘
         │ Valid
         ▼
┌──────────────────┐
│  API Request     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Authentication  │ ──────┐
│  Check           │       │ Unauthenticated
└────────┬─────────┘       ▼
         │            ┌────────────────┐
         │            │  401 Error     │
         │            └────────────────┘
         │ Authenticated
         ▼
┌──────────────────┐
│  Authorization   │ ──────┐
│  Check           │       │ Unauthorized
└────────┬─────────┘       ▼
         │            ┌────────────────┐
         │            │  403 Error     │
         │            └────────────────┘
         │ Authorized
         ▼
┌──────────────────┐
│  Query           │ ──────┐
│  Validation      │       │ Invalid query
└────────┬─────────┘       ▼
         │            ┌────────────────┐
         │            │  400 Error     │
         │            │  + Details     │
         │            └────────────────┘
         │ Valid query
         ▼
┌──────────────────┐
│  Query           │ ──────┐
│  Execution       │       │ Database error
└────────┬─────────┘       ▼
         │            ┌────────────────┐
         │            │  500 Error     │
         │            │  + Log         │
         │            └────────────────┘
         │ Success
         ▼
┌──────────────────┐
│  Transform       │ ──────┐
│  Results         │       │ Transform error
└────────┬─────────┘       ▼
         │            ┌────────────────┐
         │            │  500 Error     │
         │            └────────────────┘
         │ Success
         ▼
┌──────────────────┐
│  Return JSON     │
│  Response        │
└──────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Production Setup                      │
└─────────────────────────────────────────────────────────┘

              ┌──────────────────────┐
              │   Load Balancer      │
              │   (HTTPS)            │
              └──────────┬───────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
  ┌──────────┐   ┌──────────┐   ┌──────────┐
  │ Frontend │   │ Frontend │   │ Frontend │
  │ Instance │   │ Instance │   │ Instance │
  └────┬─────┘   └────┬─────┘   └────┬─────┘
       │              │              │
       └──────────────┼──────────────┘
                      │
                      ▼
              ┌──────────────────────┐
              │   API Gateway        │
              └──────────┬───────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
  ┌──────────┐   ┌──────────┐   ┌──────────┐
  │ Backend  │   │ Backend  │   │ Backend  │
  │ Instance │   │ Instance │   │ Instance │
  └────┬─────┘   └────┬─────┘   └────┬─────┘
       │              │              │
       └──────────────┼──────────────┘
                      │
                      ▼
              ┌──────────────────────┐
              │   PostgreSQL         │
              │   (Primary)          │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │   PostgreSQL         │
              │   (Replica)          │
              │   (Read-only)        │
              └──────────────────────┘

  Optional Additions:
  • Redis for caching query results
  • Message queue for background report execution
  • CDN for static assets
  • Monitoring & logging (Datadog, Sentry)
```

---

This architecture document provides a comprehensive visual overview of the Custom Report Builder system, showing how all components interact and flow together.
